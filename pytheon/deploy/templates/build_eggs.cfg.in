[buildout]
newest = false
prefer-final = true
extends = 
dump-picked-versions-file = versions.cfg
overwrite-picked-versions-file = true
parts = deploy
versions=versions

[deploy]
recipe = zc.recipe.egg
eggs =
